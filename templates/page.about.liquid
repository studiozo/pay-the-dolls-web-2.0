import React, { useState } from 'react';
import { Copy, Check, FileCode } from 'lucide-react';

export const LiquidExport: React.FC = () => {
  const [copied, setCopied] = useState(false);

  const liquidCode = `{% comment %}
  PTD™ UNIFIED IDENTITY PROTOCOL v1.2
  Template: page.about-identity.liquid
  Components: 00 About, 01 Protocols, 02 Identity, 03 Colors, 04 Type
{% endcomment %}

<style>
  :root {
    --ptd-phthalo: #123524;
    --ptd-mint: #00FF88;
    --ptd-white: #ffffff;
    --ptd-light-gray: #FAFAFA;
    --ptd-medium-gray: #666666;
    --ptd-border: #E8E8E8;
  }

  .ptd-page {
    background: var(--ptd-white);
    color: var(--ptd-phthalo);
    font-family: 'Avenir Next', 'Avenir', sans-serif;
    line-height: 1.6;
    padding: 120px 20px;
    -webkit-font-smoothing: antialiased;
  }

  .ptd-container { max-width: 1100px; margin: 0 auto; }

  .ptd-label {
    display: inline-block;
    font-size: 11px;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    border-bottom: 2px solid var(--ptd-phthalo);
    padding-bottom: 8px;
    margin-bottom: 40px;
  }

  .ptd-h1 {
    font-size: clamp(60px, 12vw, 100px);
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: -0.05em;
    line-height: 0.8;
    margin-bottom: 40px;
    color: var(--ptd-phthalo);
  }

  .ptd-intro {
    font-size: clamp(22px, 3.5vw, 30px);
    font-weight: 500;
    max-width: 850px;
    margin-bottom: 60px;
    line-height: 1.3;
  }

  .ptd-quote {
    background: var(--ptd-light-gray);
    border-left: 8px solid var(--ptd-phthalo);
    padding: 60px;
    margin-bottom: 140px;
  }

  .ptd-quote-txt {
    font-size: clamp(18px, 2.5vw, 26px);
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    line-height: 1.3;
  }

  .ptd-divider {
    text-align: center;
    border-top: 2px solid var(--ptd-border);
    border-bottom: 2px solid var(--ptd-border);
    padding: 120px 0;
    margin-bottom: 120px;
  }

  .ptd-h2 {
    font-size: clamp(36px, 7vw, 64px);
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: -0.03em;
  }

  .ptd-grid { display: grid; grid-template-columns: 1fr; gap: 100px; margin-bottom: 160px; }
  @media (min-width: 768px) { .ptd-grid { grid-template-columns: 1fr 1fr; gap: 120px; } }

  .ptd-card .img-wrap {
    position: relative; aspect-ratio: 3/4; overflow: hidden; margin-bottom: 40px;
    border: 2px solid transparent; transition: all 0.5s ease;
  }
  .ptd-card:hover .img-wrap { border-color: var(--ptd-mint); }
  .ptd-card img {
    width: 100%; height: 100%; object-fit: cover; filter: grayscale(100%);
    transition: all 0.8s cubic-bezier(0.165, 0.84, 0.44, 1);
  }
  .ptd-card:hover img { filter: grayscale(0%); transform: scale(1.05); }

  .ptd-tag {
    position: absolute; bottom: 0; left: 0; padding: 12px 20px;
    font-size: 11px; font-weight: 900; text-transform: uppercase; letter-spacing: 0.1em;
  }
  .ptd-tag.phthalo { background: var(--ptd-phthalo); color: white; }
  .ptd-tag.mint { background: var(--ptd-mint); color: var(--ptd-phthalo); }

  .ptd-name { font-size: 36px; font-weight: 900; text-transform: uppercase; margin-bottom: 8px; }
  .ptd-role { font-size: 12px; font-weight: 800; text-transform: uppercase; letter-spacing: 0.3em; color: var(--ptd-medium-gray); margin-bottom: 30px; }
  .ptd-bio { font-size: 18px; color: var(--ptd-medium-gray); margin-bottom: 40px; }
  .ptd-bio span { display: block; color: var(--ptd-phthalo); font-weight: 900; text-transform: uppercase; font-size: 20px; margin-top: 15px; }

  /* 01 ARIA PROTOCOLS */
  .ptd-dark-wrap {
    background: #111; color: white; padding: 140px 40px; margin: 120px -40px;
  }
  .ptd-proto-h { font-size: clamp(40px, 8vw, 70px); font-weight: 900; text-transform: uppercase; margin-bottom: 60px; }
  .ptd-proto-h span { color: var(--ptd-mint); }
  .ptd-proto-grid { display: grid; grid-template-columns: 1fr; gap: 40px; }
  @media (min-width: 900px) { .ptd-proto-grid { grid-template-columns: 1fr 2fr; } }
  .ptd-proto-card { background: #1a1a1a; padding: 50px; border: 1px solid #333; }

  .ptd-cta {
    display: inline-block; background: var(--ptd-phthalo); color: white;
    padding: 30px 70px; text-transform: uppercase; font-weight: 900;
    font-size: 14px; text-decoration: none; letter-spacing: 0.4em;
    border: 2px solid var(--ptd-phthalo); transition: all 0.3s;
  }
  .ptd-cta:hover { background: var(--ptd-mint); color: var(--ptd-phthalo); }
</style>

<div class="ptd-page">
  <div class="ptd-container">
    
    <!-- 00 // ABOUT -->
    <span class="ptd-label">00 // About</span>
    <h1 class="ptd-h1">PAY THE DØLLS™</h1>
    
    <div class="ptd-intro">
      Founded by <strong>Zoe Touloupis</strong> and <strong>Aria Brooks</strong> to build financial infrastructure for transgender artists, creators, and entrepreneurs.
    </div>

    <div class="ptd-quote">
      <div class="ptd-quote-txt">
        Creative labor deserves ownership, leverage, and control.<br>
        We built the system accordingly.
      </div>
    </div>

    <div class="ptd-divider">
      <h2 class="ptd-h2">The Yin & Yang</h2>
      <p style="font-size: 12px; font-weight: 900; text-transform: uppercase; letter-spacing: 0.3em; color: #888; margin-top: 20px;">Two aesthetics. One agenda.</p>
    </div>

    <div class="ptd-grid">
      <!-- ARIA -->
      <div class="ptd-card">
        <div class="img-wrap">
          <img src="https://images.unsplash.com/photo-1616091093729-c0463327ebcc?q=80&w=2000&auto=format&fit=crop" alt="Aria">
          <div class="ptd-tag phthalo">West Village Girl</div>
        </div>
        <h3 class="ptd-name">Aria Brooks</h3>
        <p class="ptd-role">Co-Founder / Operations Lead</p>
        <div class="ptd-bio">
          Tasteful. Controlled. Meticulous. Soft palettes, editorial restraint, and systems that don’t break under pressure.
          <span>She makes it legible.</span>
          <span>She makes it last.</span>
        </div>
      </div>

      <!-- ZOE -->
      <div class="ptd-card">
        <div class="img-wrap">
          <img src="https://images.unsplash.com/photo-1544211086-635e957388d0?q=80&w=2787&auto=format&fit=crop" alt="Zoe">
          <div class="ptd-tag mint">Warehouse Raves in Bushwick</div>
        </div>
        <h3 class="ptd-name">Zoe Touloupis</h3>
        <p class="ptd-role">Co-Founder / Creative Director</p>
        <div class="ptd-bio">
          Industrial. Confrontational. Unpolished by design. Underground art, brutalist systems, neon chaos, and momentum that doesn’t wait.
          <span>She makes it move.</span>
          <span>She makes it unavoidable.</span>
        </div>
      </div>
    </div>

    <!-- 01 // ARIA PROTOCOLS -->
    <div class="ptd-dark-wrap">
      <span class="ptd-label" style="border-color: var(--ptd-mint); color: white;">01 // Aria Protocols</span>
      <h2 class="ptd-proto-h">Agentic <span>Instructions</span></h2>
      <div class="ptd-proto-grid">
        <div class="ptd-proto-card">
          <h4 style="color: var(--ptd-mint); font-weight: 900; text-transform: uppercase; margin-bottom: 15px;">Tone & Voice</h4>
          <p style="color: #aaa; font-size: 15px;">Aria is the digital concierge. She is efficient, fashion-forward, and transaction-focused. Professionalism is infrastructure.</p>
        </div>
        <div class="ptd-proto-card">
          <h4 style="color: var(--ptd-mint); font-weight: 900; text-transform: uppercase; margin-bottom: 15px;">Visual Hierarchy</h4>
          <p style="color: #aaa; font-size: 15px;">Phthalo (#123524) anchors all primary text. Mint (#00FF88) is strictly for forced movement and active states.</p>
        </div>
      </div>
    </div>

    <div style="text-align: center; padding: 120px 0;">
       <h3 style="font-size: clamp(32px, 8vw, 70px); font-weight: 900; text-transform: uppercase; margin-bottom: 60px;">This isn’t support.<br><span style="color: #999;">It’s power.</span></h3>
       <a href="/pages/submit" class="ptd-cta">Join the Platform</a>
    </div>

  </div>
</div>`;

  const copyToClipboard = () => {
    navigator.clipboard.writeText(liquidCode);
    setCopied(true);
    setTimeout(() => setCopied(false), 2000);
  };

  return (
    <div className="bg-[#1a1a1a] border-2 border-[#333] rounded-lg overflow-hidden font-sans">
      <div className="p-6 border-b-2 border-[#333] flex justify-between items-center bg-[#222]">
        <div className="flex items-center gap-4">
          <FileCode className="w-8 h-8 text-mint" />
          <span className="text-xs font-black text-white uppercase tracking-[3px]">ptd-unified-v1.2.liquid</span>
        </div>
        <button 
          onClick={copyToClipboard}
          className="flex items-center gap-3 bg-phthalo text-white px-8 py-4 rounded-sm text-xs font-black uppercase tracking-[3px] hover:bg-mint hover:text-phthalo transition-all duration-300"
        >
          {copied ? <Check className="w-4 h-4" /> : <Copy className="w-4 h-4" />}
          {copied ? 'Copied to Clipboard' : 'Copy Template'}
        </button>
      </div>
      <div className="p-10 max-h-[800px] overflow-y-auto bg-[#0a0a0a]">
        <pre className="text-[14px] text-gray-400 font-mono leading-relaxed overflow-x-auto selection:bg-mint selection:text-phthalo">
          {liquidCode}
        </pre>
      </div>
    </div>
  );
};